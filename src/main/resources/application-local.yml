server:
  port: 8082

logging.file: logs/application.log
logging:
  level:
      org:
        springframework:
          web: DEBUG
        hibernate: ERROR
spring:
  datasource:
    url:  jdbc:h2:mem:./h2/org
  h2:
    console:
      enabled: true